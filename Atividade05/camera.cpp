#include "camera.h"

// Nenhuma implementação necessária neste arquivo, já que a classe Camera possui apenas o construtor e o método get_ray().
// Todas as definições foram feitas diretamente no arquivo header.